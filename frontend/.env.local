# CareIQ Frontend — Environment Variables
# Copy this to .env.local and adjust for your environment.

# API base URL (FastAPI backend)
VITE_API_URL=http://localhost:8000

# Use mock data (no backend required — set to 'true' for offline development)
VITE_USE_MOCK=true
